# Beispiel-Entscheidung C — Bürostuhl-Farbe (erfunden)

**Erfunden, nicht kopiert.** Fixture für `tools/entscheidungen-abloese-pruefen.js` —
der TOTE VERWEIS: das genannte Ziel kommt unter den Fixture-Dokumenten nicht vor,
genau wie beim `codeberg-entfaellt`-Fund (Vorgänger-Datei nicht mehr auffindbar).

**Ersetzt:** dok-nichtvorhanden.md

## Entscheidung

Die Bürostuhl-Farbe wird künftig grau statt blau.
