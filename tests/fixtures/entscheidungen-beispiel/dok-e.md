# Beispiel-Entscheidung E — alte Pausenraum-Kaffeemaschine (erfunden)

**Erfunden, nicht kopiert.** Fixture für `tools/entscheidungen-abloese-pruefen.js` —
trägt BEWUSST kein `Ersetzt durch:`-Feld, obwohl dok-d.md diese Datei als
abgelöst führt. Das ist der Befund, nicht ein Versehen der Fixture.

## Entscheidung (überholt, aber unvermerkt)

Die alte Kaffeemaschine war Modell X.
