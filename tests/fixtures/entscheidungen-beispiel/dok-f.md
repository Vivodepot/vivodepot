# Beispiel-Entscheidung F — Parkplatzordnung (erfunden)

**Erfunden, nicht kopiert.** Fixture für `tools/entscheidungen-abloese-pruefen.js` —
der SONDERWERT-Fall: die Vorgänger-Datei ist nicht mehr auffindbar, darum
„unbekannt" statt eines Dateinamens (analog `codeberg-entfaellt-2026-07-31.md`).
Zählt als AUSGENOMMEN, nicht als toter Verweis.

**Ersetzt:** unbekannt — die vormals hier genannte Vorgänger-Datei ist im
Fixture-Bestand nicht mehr auffindbar, auch nicht archiviert.

## Entscheidung

Die Parkplatzordnung wird überarbeitet.
