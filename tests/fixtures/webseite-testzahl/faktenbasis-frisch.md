# Faktenbasis — Fixture

**Erzeugt am:** 2027-01-10 · **Commit:** `fixture0000` · **Werkzeug:** `tools/faktenbasis-erzeugen.js`

## Prüfebene

- Suite (Node-Tests, echter Lauf `node --test`, TAP-Summenzeile): 6300
- E2E (Playwright): 170 `test(`-Aufrufe in `tests/e2e/*.spec.js` + 4 aus Schleifen über CPU-Drosselungen = **174 ausgeführte Tests** (mechanisch gezählt, nicht ausgeführt — die Differenz ist konstant, s. A423)
- Wächter-Register (`tools/waechter-register.js`): 80
